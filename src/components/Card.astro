---
interface Props {
  title: string;
  body: string;
  href: string;
}

const { href, title, body } = Astro.props;
---

<li class="link-card">
  <a href={href}>
    <h2>
      {title}
      <span>&rarr;</span>
    </h2>
    <p>
      {body}
    </p>
  </a>
</li>
<style>
  .link-card {
    list-style: none;
    display: flex;
    padding: 1px;
    background-color: white;
    border: 1px solid rgb(var(--pro-accent));
    background-size: 400%;
    border-radius: 7px;
    background-position: 100%;
    transition: background-position 0.6s cubic-bezier(0.22, 1, 0.36, 1);
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.1);
  }
  .link-card > a {
    width: 100%;
    text-decoration: none;
    line-height: 1.4;
    padding: calc(1.5rem - 1px);
    border-radius: 8px;
    /* background-color: #102eed; */
    /* background-color: var(--pro-accent-gradient); */
    /* background-color: white; */
    opacity: 0.8;
  }
  h2 {
    margin: 0;
    font-size: 1.25rem;
    color: rgb(var(--pro-accent));
    transition: color 0.6s cubic-bezier(0.22, 1, 0.36, 1);
  }
  p {
    margin-top: 0.5rem;
    margin-bottom: 0;
  }
  .link-card:is(:hover, :focus-within) {
    background-position: 0;
    background-image: var(--pro-accent-gradient);
    color: white;
  }
  .link-card:is(:hover, :focus-within) h2 {
    color: rgb(var(--pro-accent-light));
  }
</style>
